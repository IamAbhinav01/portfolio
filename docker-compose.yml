services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio-frontend
    ports:
      - '3000:3000'
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - portfolio-network

  backend:
    build:
      context: ./contact-form-backend
      dockerfile: Dockerfile
    container_name: portfolio-backend
    ports:
      - '5000:5000'
    restart: unless-stopped
    env_file:
      - ./contact-form-backend/.env
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
